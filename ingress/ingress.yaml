apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: micro-ingress

spec:
  rules:
  - host: app.local.com
    http:
      paths:

      - path: /
        pathType: Prefix
        backend:
          service:
            name: gateway-service
            port:
              number: 80